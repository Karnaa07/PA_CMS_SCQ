


/contact:
  controller: main
  action: contact

/login:
  controller: user
  action: login

/media:
  controller: admin
  action: media

/pages:
  controller: admin
  action: pages_settings

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/forget:
  controller: user
  action: pwdforget

/:
  controller: main
  action: home

/dashboard:
  controller: main
  action: home

